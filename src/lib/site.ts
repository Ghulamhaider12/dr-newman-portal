/**
 * The canonical public origin. Single source of truth for absolute URLs used in
 * metadata (metadataBase / canonicals), the sitemap, and robots.txt — so search
 * engines always attribute content to the real domain, not a proxy mirror.
 */
export const SITE_ORIGIN = 'https://advice4docs.com';
